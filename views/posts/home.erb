<div class="bg-white border-gray-200 dark:bg-gray-900 w-full flex flex-col items-center py-10">
  <!-- Container for posts -->
  <div id="post-container" class="max-w-96 min-w-20 flex flex-col gap-10">
    <%=PostListComponent(@posts, @user_id, @offset + @limit)%>
  </div>
</div>